/** Read server-side by app/layout.tsx via next/headers' cookies() — keep these in sync with useTheme.ts / useLocale.ts. */
export const THEME_COOKIE_KEY = 'panorama.theme'
export const LOCALE_COOKIE_KEY = 'panorama.locale'
