{
  "name": "token-system",
  "version": "1.0.0",
  "description": "Production-ready design token system with multi-theme support using Style Dictionary",
  "type": "module",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:tokens": "node config.js --verbose",
    "build:tokens:light": "yarn build:tokens -- --theme=light",
    "build:tokens:dark": "yarn build:tokens -- --theme=dark",
    "build:tokens:high-contrast": "yarn build:tokens -- --theme=high-contrast",
    "prepublishOnly": "yarn build:tokens"
  },
  "devDependencies": {
    "minimist": "^1.2.8",
    "sass": "^1.97.2",
    "storybook": "^10.1.11",
    "style-dictionary": "^5.1.4"
  },
  "files": [
    "light/build",
    "dark/build",
    "high-contrast/build",
    "core"
  ]
}
